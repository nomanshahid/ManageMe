<%= form_for(@absent) do |f| %>
  <% if @absent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@absent.errors.count, "error") %> prohibited this absent from being saved:</h2>

      <ul>
      <% @absent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
